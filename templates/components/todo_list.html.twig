<div {{ init_live_component(this) }}>
	{{ component('create_task_form', {
        task: this.task,
        form: this.form
    }) }}

	{% for task in this.tasks %}
		<br>{{ task.text }}
	{% endfor %}
</div>
